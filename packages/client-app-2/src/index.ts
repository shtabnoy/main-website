import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return (
        <div>
            <h1>Client App 2</h1>
            <p>This is the second client application.</p>
        </div>
    );
};

ReactDOM.render(<App />, document.getElementById('root'));