{
  "name": "my-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "start": "yarn workspace server-app start"
  },
  "devDependencies": {
    "typescript": "^4.5.0"
  }
}